<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Audio Upload</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: #f7f9fc;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 40px;
        }
        
        .upload-box {
            background: white;
            padding: 30px;
            border-radius: 12px;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
            max-width: 600px;
            width: 100%;
            margin-bottom: 40px;
        }
        
        .form-control {
            border-radius: 6px;
        }
        
        .btn-upload {
            width: 100%;
            padding: 10px;
            border-radius: 6px;
            font-weight: 500;
        }
        
        .gallery audio {
            width: 100%;
            outline: none;
        }
    </style>
</head>

<body>

    <!-- Upload Form -->
    <div class="upload-box">
        <h2 class="text-center">Upload Your Audios</h2>
        <form action="/upload" method="POST" enctype="multipart/form-data">
            <div class="mb-3">
                <input class="form-control" type="file" name="file" id="file" accept="audio/*" required>
            </div>
            <button type="submit" class="btn btn-primary btn-upload">Upload</button>
        </form>
    </div>

    <!-- Audio Gallery -->
    <% if (audios && audios.length > 0) { %>
        <h3 class="mb-4">Uploaded Audios</h3>
        <div class="row gallery">
            <% audios.forEach(url => { %>
                <div class="col-md-6 mb-4">
                    <audio controls>
                        <source src="<%= url %>" type="audio/mpeg">
                        Your browser does not support the audio element.
                    </audio>
                    <p class="mt-2 text-muted small">
                        <%= url %>
                    </p>
                </div>
                <% }) %>
        </div>
        <% } else { %>
            <p class="text-muted">No audios uploaded yet.</p>
            <% } %>

                <script>
                    // Reset file input on load
                    window.onload = () => {
                        document.getElementById("file").value = "";
                    };
                </script>
</body>

</html>